---
captured: 2026-05-13
source: developers.snap.com/lens-studio/ (5.x current)
source_format: Docusaurus v3.9.2 raw markdown endpoints (URL + ".md")
files_must_have: 300
files_nice_to_have: 64
files_other: 1 (home.md)
files_total: 365
size_total_mb: 5.0
curation_scope: Custom Snapchat filter design via Claude Code + MCP integration
excluded_count: 724
fetch_404_count: 9
fetch_404_zone: outside must-have, no action needed
cache_location: ~/Projects/adidas-lens/snap-docs-cache/ (11 MB, retained for re-curation)
captured_by: Niklaz Hallberg / Valtech Radon
---

# Snap Lens Studio docs mirror — metadata

Curated mirror of Snap Lens Studio 5.x developer documentation, scoped for **Custom Snapchat filter design via Claude Code + MCP integration**. Lives alongside other references in the `lens-studio-snapchat-filter` skill.

## What's here (365 files, 5.0 MB)

**Must-have (300 files):**
- `01-mcp-and-claude-code/` (10) — MCP + Claude Code setup (HIGHEST PRIO)
- `02-scripting-api/` (36) — Scripting API, animation, audio
- `03-assets-pipeline/` (79) — 2D/3D pipelines, asset library, graphics, package mgmt
- `04-scene-and-components/` (40) — LS interface, scene setup, interactivity, camera, text, UI
- `05-ar-tracking/` (90) — face, body, hand, world tracking (all body parts)
- `06-try-on/` (8) — sneakers, eyewear, jewelry, clothing, watches
- `07-publishing/` (16) — submission, sponsored, distribution
- `08-performance/` (8) — FPS, memory, lens-size, performance toolkit
- `09-overview/` (14) — getting started, what-is-LS, migrating from other tools

**Nice-to-have (64 files):**
- `nice-to-have/snap-ml/` (18), `genai-suite/` (18), `physics/` (9), `remote-apis/` (6), `persistent-storage/` (6), `lens-cloud/` (5), `prompt-response/` (2)

## What's NOT here

Excluded categories (724 files in upstream sitemap, kept only in `snap-docs-cache/`):

- **Material Editor Nodes** (250) — too narrow; we don't use Material Editor
- **VFX Editor Nodes** (264) — too narrow; we don't use VFX Editor
- **Visual Scripting Nodes** (114) — we use TypeScript, not Visual Scripting
- **Tutorials** (7) + **Examples** (27) — we learn from docs, not exemplars
- **Plugin Development** (12) — we don't build plugins
- **Bitmoji Suite/Avatar** (12), **Games** (13), **Connected Lenses** (7), **Location AR** (6), **User Context** (5), **Voice ML** (1), **Lens Creator Rewards / Monetization** (3) — off-scope
- **4.55.1 deprecated docs** (970 — skipped entirely; 5.x supersedes)

## Routing

See `00-INDEX.md` for the full routing table from frågetyp → folder.

## Source provenance

Each `.md` file is fetched verbatim from Snap's raw markdown endpoint (`https://developers.snap.com/lens-studio/<path>.md`). Internal links use absolute `/lens-studio/...` paths pointing back at the live docs site. Image refs (`/img/lens-studio/...`) do NOT resolve in this mirror — text content is the value here, not embedded media.

## Maintenance

**Re-mirror trigger:** quarterly, OR after major LS-version bump (e.g., 5.x → 6.x), OR when a specific page becomes load-bearing and seems stale.

**Re-mirror procedure:**
1. Re-run the curl-loop in `~/Projects/adidas-lens/snap-docs-cache/` (see fetch script — currently inline in the conversation that produced this mirror; a stand-alone `.refetch.sh` is a future task)
2. Re-run the Python copy-script (also inline in original session) to re-curate cache → skill
3. Diff old vs new to spot meaningful changes
4. Commit as a `chore(v0.6.x):` patch

## Bug log (transparent)

- 2026-05-13 initial Fas 1 copy: `home.md` missed by classifier (UNCLASSIFIED bucket + format-mismatch). Fixed in same session by manual copy.
